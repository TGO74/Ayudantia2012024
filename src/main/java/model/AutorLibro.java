package model;

import java.time.LocalDate;

public class AutorLibro extends Autor {

    public AutorLibro(String nombre, LocalDate fechaNacimiento) {
        super(nombre, fechaNacimiento);
    }
}
