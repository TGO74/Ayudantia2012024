package model;

import java.time.LocalDate;

public class AutorComic extends Autor {

    public AutorComic(String nombre, LocalDate fechaNacimiento) {
        super(nombre, fechaNacimiento);
    }
}
