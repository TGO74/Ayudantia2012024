package model;

import java.time.LocalDate;

public class AutorPoema extends Autor {

    public AutorPoema(String nombre, LocalDate fechaNacimiento) {
        super(nombre, fechaNacimiento);
    }
}
